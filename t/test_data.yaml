---
# textfield: DISCUSS
-PAGE_002:  website eq 'google.com'

# select/choice/multichoice
-PAGE_003:  why_choose_unilodge == 5
-PAGE_004:  why_choose_unilodge == 6

# int/float
-PAGE_007:  female_attendance + male_attendance < 50
-PAGE_008:  male_attendance != 13
-PAGE_009:  female_attendance > 100

# something more complicated
# - ( ( female_attendance / ( female_attendance + male_attendance ) ) > 90 ) or ( ( male_attendance / ( female_attendance + male_attendance ) ) > 90 ): PAGE_010
# NOTE: it still needs to be something that YAML can use as a hash key
# so something more like this will work
-PAGE_010:  female_attendance / total_attendance > .5 or male_attendance / total_attendance > .5 
# or better make a method of it:
-PAGE_010:  female_percentage > 50 or male_percentage > 50 

-PAGE_005:  default
